{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter $CSA_APP build"
  },
  "deploy": {
    "preDeployCommand": "sh -c 'if [ \"$CSA_APP\" = \"@csa/admin\" ]; then node scripts/railway-migrate.mjs; else echo \"[pre-deploy] $CSA_APP has no database; skipping migrations\"; fi'",
    "startCommand": "pnpm --filter $CSA_APP exec next start --port $PORT --hostname 0.0.0.0",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
