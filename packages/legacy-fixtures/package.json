{
  "name": "@csa/legacy-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Synthetic legacy estate for the CSA migration harness. Fictional data only — no CSA system is read by any of this.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate": "tsx src/cli.ts"
  },
  "dependencies": {
    "@csa/domain": "workspace:*"
  }
}
